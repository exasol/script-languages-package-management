name: Build Matrix (Binary Integration Tests)

on:
  workflow_call:
    outputs:
      matrix:
        description: "Generates the binary integration test config build matrix"
        value: ${{ jobs.matrix_runner.outputs.matrix }}

jobs:
  matrix_runner:
    runs-on: "ubuntu-24.04"
    permissions:
      contents: read
    steps:
      - name: SCM Checkout
        uses: actions/checkout@v6

      - name: Setup Python & Poetry Environment
        uses: exasol/python-toolbox/.github/actions/python-environment@v7
        with:
          python-version: "3.10"
          poetry-version: "2.3.0"

      - name: Generate matrix
        run: poetry run -- nox -s matrix:binary-int-test-config

      - id: set-matrix
        run: |
          echo "matrix=$(poetry run -- nox -s matrix:binary-int-test-config)" >> $GITHUB_OUTPUT

    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
